$schema: "http://json-schema.org/draft-07/schema#"

description: Cluster File Schema

properties:
  sample:
    type: string
    description: Representative sample name for given cluster
  cluster_name:
    type: string
    description: Name of the cluster in the externa database.
  required:
    - sample
    - cluster_name
